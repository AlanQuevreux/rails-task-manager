<h1>My Tasks</h1>

<%# Liste de taches avec lien pour détails de la tache %>

<div>
  <% @tasks.each do |task| %>
    <p><%= link_to task.title, task_path(task) %></p>
  <% end %>
</div>

<p> <%=link_to "Add a new task", new_task_path %></p>
